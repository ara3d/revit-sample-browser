# {ClassName}

| Field | Value |
|-------|-------|
| **Sample** | {SampleFolder} |
| **Class** | `{FullClassName}` |
| **Source** | `src/{SourcePath}` |
| **SDK ReadMe** | `src/{ReadMePath}` *(if present)* |
| **MCP rating** | {McpRating}/5 |

{One sentence stating what this command does.}

## What it demonstrates

- {API pattern or Revit feature — bullet, not paragraph}
- {Key types/methods}

## Prerequisites

{Document type, template, selection, or external files. Omit section if none.}

## User interaction

{Dialogs, picks, or ribbon-only flow. State whether headless execution is feasible.}

## MCP notes

{Only when MCP ≥ 3: proposed tool name, parameters, return value, and UI refactoring needed. For MCP ≤ 2: one line on why it is or is not a good tool candidate.}

## See also

- MCP descriptor: `docs/mcp/{McpPath}` *(MCP ≥ 3 only)*
- {Link to related command docs in this repo, if any}
