{
  "name": "revit_add_analytical_physical_associations",
  "description": "Associates multiple analytical elements with multiple physical elements in one operation.",
  "arguments": {
    "type": "object",
    "properties": {
      "analytical_element_ids": {
        "type": "array",
        "items": { "type": "integer" },
        "description": "Analytical element ids to associate"
      },
      "physical_element_ids": {
        "type": "array",
        "items": { "type": "integer" },
        "description": "Physical element ids to associate"
      }
    },
    "required": ["analytical_element_ids", "physical_element_ids"]
  },
  "commandClass": "AddCustomAssociation",
  "sample": "ContextualAnalyticalModel",
  "source": "src/ContextualAnalyticalModel/AddCustomAssociation.cs",
  "mcpRating": 4,
  "requiresUi": false,
  "notes": "Uses AddAssociation overload accepting ISet<ElementId>; replace multi-pick UI."
}
