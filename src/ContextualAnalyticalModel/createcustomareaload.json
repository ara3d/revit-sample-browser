{
  "name": "revit_create_area_load",
  "description": "Creates a uniform area load on an analytical host within a rectangular boundary.",
  "arguments": {
    "type": "object",
    "properties": {
      "host_element_id": {
        "type": "integer",
        "description": "Analytical host element id"
      },
      "boundary_corners": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": { "x": { "type": "number" }, "y": { "type": "number" }, "z": { "type": "number" } },
          "required": ["x", "y", "z"]
        },
        "minItems": 2,
        "maxItems": 2,
        "description": "Diagonal corners of the rectangular load boundary"
      },
      "force_vector": {
        "type": "object",
        "properties": { "x": { "type": "number" }, "y": { "type": "number" }, "z": { "type": "number" } },
        "required": ["x", "y", "z"],
        "description": "Uniform force vector applied to the area load"
      },
      "load_case_id": {
        "type": "integer",
        "description": "Optional load case element id"
      }
    },
    "required": ["host_element_id", "boundary_corners", "force_vector"]
  },
  "commandClass": "CreateCustomAreaLoad",
  "sample": "ContextualAnalyticalModel",
  "source": "src/ContextualAnalyticalModel/CustomAreaLoad.cs",
  "mcpRating": 4,
  "requiresUi": false,
  "notes": "Replace picks with ids and coordinates; validate boundary inside host."
}
