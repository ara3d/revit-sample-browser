{
  "name": "revit_duplicate_graphics_overlay",
  "description": "Draws DirectContext3D duplicates of element graphics at a specified offset without creating new elements.",
  "arguments": {
    "type": "object",
    "properties": {
      "element_ids": {
        "type": "array",
        "items": { "type": "integer" },
        "description": "Elements whose graphics should be duplicated"
      },
      "offset": {
        "type": "object",
        "properties": { "x": { "type": "number" }, "y": { "type": "number" }, "z": { "type": "number" } },
        "description": "Translation offset for overlay graphics (default 0,0,45)"
      }
    },
    "required": ["element_ids"]
  },
  "commandClass": "CommandDuplicateGraphics",
  "sample": "DuplicateGraphics",
  "source": "src/DuplicateGraphics/Command.cs",
  "mcpRating": 4,
  "requiresUi": true,
  "notes": "Replace PickObjects with element_ids; requires DuplicateGraphics Application OnStartup."
}
