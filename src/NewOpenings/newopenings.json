{
  "name": "revit_create_opening",
  "description": "Creates an opening in a selected wall or floor from a closed profile loop.",
  "arguments": {
    "type": "object",
    "properties": {
      "host_element_id": {
        "type": "integer",
        "description": "Wall or floor element id to receive the opening"
      },
      "profile_points": {
        "type": "array",
        "items": {
          "type": "array",
          "items": { "type": "number" },
          "minItems": 3,
          "maxItems": 3
        },
        "description": "Closed polygon vertices in feet (XY plane relative to host sketch)"
      }
    },
    "required": ["host_element_id", "profile_points"]
  },
  "commandClass": "Command",
  "sample": "NewOpenings",
  "source": "src/NewOpenings/command.cs",
  "mcpRating": 4,
  "requiresUi": true,
  "notes": "ProfileWall and ProfileFloor contain host-specific opening creation; bypass NewOpeningsForm sketch UI for MCP."
}
