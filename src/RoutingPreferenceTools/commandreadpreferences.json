{
  "name": "revit_import_routing_preferences",
  "description": "Imports pipe routing preferences, segments, schedules, and fitting rules from RoutingPreference Builder XML.",
  "arguments": {
    "type": "object",
    "properties": {
      "xml_file_path": {
        "type": "string",
        "description": "Path to a validated RoutingPreference Builder XML file"
      }
    },
    "required": ["xml_file_path"]
  },
  "commandClass": "CommandReadPreferences",
  "sample": "RoutingPreferenceTools",
  "source": "src/RoutingPreferenceTools/RoutingPreferenceBuilder/CommandReadPreferences.cs",
  "mcpRating": 5,
  "requiresUi": true,
  "notes": "Bypass OpenFileDialog; validate XML against RoutingPreferenceBuilderData.xsd before import."
}
