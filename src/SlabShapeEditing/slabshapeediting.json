{
  "name": "revit_edit_slab_shape",
  "description": "Adds or modifies slab shape vertices and creases on a selected floor.",
  "arguments": {
    "type": "object",
    "properties": {
      "floor_id": {
        "type": "integer",
        "description": "Floor element id to edit"
      },
      "vertices": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "x": { "type": "number" },
            "y": { "type": "number" },
            "z": { "type": "number" }
          },
          "required": ["x", "y", "z"]
        },
        "description": "Vertices to add or move"
      },
      "creases": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "start": { "type": "integer" },
            "end": { "type": "integer" }
          },
          "required": ["start", "end"]
        },
        "description": "Crease definitions as vertex index pairs"
      }
    },
    "required": ["floor_id"]
  },
  "commandClass": "Command",
  "sample": "SlabShapeEditing",
  "source": "src/SlabShapeEditing/Command.cs",
  "mcpRating": 4,
  "requiresUi": true,
  "notes": "SlabShapeEditor API is separable; bypass SlabShapeEditingForm mouse drawing."
}
