{
  "name": "revit_create_rebar_text_note",
  "description": "Creates a text note labeling a selected rebar near the start of its centerline.",
  "arguments": {
    "type": "object",
    "properties": {
      "rebar_id": {
        "type": "integer",
        "description": "Rebar element id"
      },
      "text": {
        "type": "string",
        "description": "Optional label text; default uses category and name"
      },
      "view_id": {
        "type": "integer",
        "description": "View id for the text note"
      }
    },
    "required": ["rebar_id", "view_id"]
  },
  "commandClass": "CreateText",
  "sample": "TagBeam",
  "source": "src/TagBeam/Command.cs",
  "mcpRating": 4,
  "requiresUi": false,
  "notes": "Selection-based in sample; pass rebar_id and view_id for automation."
}
