{
  "name": "revit_get_units",
  "description": "Reads project unit formatting settings including per-discipline spec format options and decimal grouping.",
  "arguments": {
    "type": "object",
    "properties": {
      "discipline": {
        "type": "string",
        "description": "Optional discipline label to filter returned measurable specs"
      }
    },
    "required": []
  },
  "commandClass": "Command",
  "sample": "Units",
  "source": "src/Units/CS/Command.cs",
  "mcpRating": 5,
  "requiresUi": false,
  "notes": "Pair with revit_set_units for writes; bypass UnitsForm and FormatForm using Document.GetUnits and FormatOptions directly."
}
