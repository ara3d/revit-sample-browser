{
  "name": "revit_example_tool",
  "description": "One sentence describing what this tool does for an LLM caller.",
  "arguments": {
    "type": "object",
    "properties": {
      "example_param": {
        "type": "string",
        "description": "What this parameter controls"
      }
    },
    "required": []
  },
  "commandClass": "Command",
  "sample": "SampleFolder",
  "source": "src/SampleFolder/Command.cs",
  "mcpRating": 4,
  "requiresUi": false,
  "notes": "Optional: refactoring needed, prerequisites, or return shape not expressible in JSON Schema"
}
